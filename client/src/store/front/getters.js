export const studentInfo = state => state.student

export const companyInfo = state => state.company

export const chatList = state => state.chatlist

export const currentChator = state => state.currentChator

export const message = state => state.message

export const msgList = state => state.msglist

export const login = state => state.login

export const show = state => state.showChat

export const role = state => state.role